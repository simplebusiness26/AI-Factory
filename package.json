{
  "name": "ai-factory",
  "version": "0.2.0",
  "private": true,
  "description": "Mission Control + Project Brain for the AI Factory",
  "scripts": {
    "start": "node server.js",
    "dev": "wrangler dev",
    "dev:node": "node --watch server.js",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
